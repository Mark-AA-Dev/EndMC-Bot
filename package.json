{
  "name": "EndMC-Bot",
  "version": "1.0",
  "main": "main.js",
  "license": "MIT",
  "scripts": {
    "start": "node main.js",
    "dev": "../mbot/node_modules/.bin/nodemon main.js",
    "format": "docker run --rm -v $(pwd):/app -w /app node:18.8 sh -c \"npm install -g prettier && prettier --write .\"",
    "check": "docker run --rm -v $(pwd):/app -w /app node:18.8 sh -c \"npm install -g prettier && prettier --check .\""
  },
  "dependencies": {
    "ascii-table": "^0.0.9",
    "date-fns": "^3.6.0",
    "discord-html-transcripts": "^3.2.0",
    "discord.js": "^14.14.1",
    "dotenv": "^16.0.3",
    "fs": "^0.0.1-security",
    "glob": "^8.0.3",
    "mongoose": "^8.3.2",
    "ms": "^2.1.3",
    "yaml": "^2.3.2"
  }
}
